https://github.com/ThiraphatNice/ShoeStore.git

git init
git add .
git commit -m "Update dd-mm-yyyy-tttt"
git branch -M main
git remote add origin "https://..."

***git remote -v (ใชในการตรวจสอบ origin ที่ทำการเพิ่มเขาไป)***
***git remote rm origin (ใชในการลบ remote origin)***

git status

***
git fetch origin
git push --force-with-lease origin main
***

git push origin main
git pull origin main

---------------------

รายละเอียด Requirements โปรเจคต์: ร้านรองเท้า (รองเท้าแตะ/รองเท้าผ้าใบ)

1. โปรโมชัน (Promotions)
-สมาชิกใหม่รับคูปองส่วนลด 50% 
-ซื้อรองเท้าตั้งแต่ 2 คู่ขึ้นไป ลดเพิ่มคู่ละ 10% 
-บริการจัดส่งฟรีเมื่อซื้อสินค้าตามราคาที่กำหนด 
-ซื้อรองเท้ารุ่นคอลแลบพิเศษรับสินค้าของแถมลิมิเต็ด (จำกัดเวลาขาย)
-โปรโมชันลดส่งท้ายปี สูงสุด 70%

2. โครงสร้างผู้ใช้งาน (User Roles)
-ลูกค้า : กลุ่มลูกค้าทั่วไป/ผู้ใช้งานหลักของระบบ
-เจ้าของร้าน/เจ้าของร้าน : แอดมิน ผู้ดูแลภาพรวมธุรกิจ 
-พนักงานสต็อกสินค้า : เช็กและนับจำนวนสินค้า 
-พนักงานดูแลระบบ : จัดการดูแลความเรียบร้อยของเว็บไซต์
-พนักงานขาย : คิดโปรโมชัน
-พนักงานแพ็คสินค้า : จัดการเรื่องการแพ็คและจัดส่งสินค้า

3. ความต้องการด้านระบบ (System Requirements)

ฝั่งลูกค้า (Customer)
-ระบบสมัครสมาชิกใหม่ (Register)/เข้าสู่ระบบ (Login) เพื่อเข้าใช้งานระบบได้
-ระบบแสดงตัวอย่างสินค้าและราคา (ทั้งราคาก่อนลดและราคาโปรโมชัน) 
-ระบบชำระเงินผ่านธนาคารและบัตรเครดิต 
-ระบบส่วนลด/คูปอง
-ระบบติดตามสถานะการขนส่ง
-ระบบเลือกสินค้าลงตะกร้าหรือรายการโปรด
-ระบบประวัติการเข้าดูสินค้าล่าสุด
-ระบบค้นหาสินค้าและสามารถแยกหมวดหมู่สินค้าได้อย่างชัดเจน 

ฝั่งพนักงาน (Staff) :
-ระบบนับจำนวนสินค้าคงคลังแบบเรียลไทม์ 
-ระบบแบ่งโรล แยกการทำงานของพนักงานี่ทำหน้าที่แตกต่างกัน
-ระบบพยากรณ์แนวโน้มการขายสินค้า 
-ระบบสรุปยอดขายแบบรายเดือนและรายปี 
-ระบบสรุปรายการสินค้ายอดนิยม
 
 
---------------------------

CREATE TABLE roles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    role_name VARCHAR(50) NOT NULL UNIQUE
);

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    role_id INT NOT NULL,
    fullname VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    phone VARCHAR(20),
    address TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (role_id) REFERENCES roles(id)
);

CREATE TABLE categories (
    id INT AUTO_INCREMENT PRIMARY KEY,
    category_name VARCHAR(100) NOT NULL
);

CREATE TABLE products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    category_id INT NOT NULL,
    product_name VARCHAR(150) NOT NULL,
    description TEXT,
    price DECIMAL(10,2) NOT NULL,
    discount_percent DECIMAL(5,2) DEFAULT 0,
    is_limited BOOLEAN DEFAULT FALSE,
    stock_total INT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (category_id) REFERENCES categories(id)
);

CREATE TABLE product_variants (
    id INT AUTO_INCREMENT PRIMARY KEY,
    product_id INT NOT NULL,
    size VARCHAR(10),
    color VARCHAR(50),
    stock_quantity INT DEFAULT 0,
    FOREIGN KEY (product_id) REFERENCES products(id)
);

CREATE TABLE coupons (
    id INT AUTO_INCREMENT PRIMARY KEY,
    coupon_code VARCHAR(50) UNIQUE NOT NULL,
    discount_percent DECIMAL(5,2),
    min_purchase DECIMAL(10,2) DEFAULT 0,
    start_date DATETIME,
    end_date DATETIME
);

CREATE TABLE cart_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    product_variant_id INT NOT NULL,
    quantity INT NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variants(id)
);

CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    total_amount DECIMAL(10,2),
    discount_amount DECIMAL(10,2) DEFAULT 0,
    final_amount DECIMAL(10,2),
    coupon_id INT NULL,
    order_status VARCHAR(50) DEFAULT 'Pending',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (coupon_id) REFERENCES coupons(id)
);

CREATE TABLE order_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT NOT NULL,
    product_variant_id INT NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    quantity INT NOT NULL,
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (product_variant_id) REFERENCES product_variants(id)
);

CREATE TABLE payments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT NOT NULL,
    payment_method VARCHAR(50),
    payment_status VARCHAR(50) DEFAULT 'Pending',
    paid_at DATETIME,
    FOREIGN KEY (order_id) REFERENCES orders(id)
);

CREATE TABLE shipments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT NOT NULL,
    tracking_number VARCHAR(100),
    shipping_status VARCHAR(50) DEFAULT 'Preparing',
    FOREIGN KEY (order_id) REFERENCES orders(id)
);

ALTER TABLE users
ADD COLUMN uid VARCHAR(50) UNIQUE AFTER id;

INSERT INTO roles (id, role_name) VALUES
(1,'Admin'),
(2,'Users'),
(3,'Staff Stock'),
(4,'Staff Manage'),
(5,'Staff Sell'),
(6,'Staff Express');

INSERT INTO users 
(uid, role_id, fullname, email, password_hash, phone, address)
VALUES
('ADM-0001',1,'System Admin','admin@shoestore.com','admin123','0800000000','Head Office'),

('USR-0001',2,'Somchai User','somchai@email.com','123456','0811111111','Bangkok'),
('USR-0002',2,'Suda User','suda@email.com','123456','0822222222','Chiangmai'),

('STK-0001',3,'Stock Staff','stock@shoestore.com','123456','0833333333','Warehouse'),

('MNG-0001',4,'Manager Staff','manager@shoestore.com','123456','0844444444','Head Office'),

('SEL-0001',5,'Sales Staff','sales@shoestore.com','123456','0855555555','Bangkok Shop'),

('EXP-0001',6,'Express Staff','express@shoestore.com','123456','0866666666','Shipping Center');

ALTER TABLE users
DROP COLUMN uid;

---------------------------

Package

dotnet add package Microsoft.EntityFrameworkCore --version 9.0.0
dotnet add package Microsoft.EntityFrameworkCore.Design --version 9.0.0
dotnet add package Microsoft.EntityFrameworkCore.Tools --version 9.0.0
dotnet add package Pomelo.EntityFrameworkCore.MySql

dotnet tool install --global dotnet-ef

dotnet ef dbcontext scaffold "server=localhost;port=3305;database=shoe_store;user=root;password=12345;" Pomelo.EntityFrameworkCore.MySql -o Models/db