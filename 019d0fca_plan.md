## Profile Experience
- **View models**: Create `ViewModels/Account/ProfilePageViewModel.cs` (read-only data shown on the card) plus `ProfileEditViewModel` (FullName, Email, Phone, Address, optional Password + ConfirmPassword). Keep `[Required]`, `[EmailAddress]`, `[StringLength]`, and `[Compare]` attributes close to the properties.
- **Controller**: In `Controllers/AccountController.cs`, keep `GET Profile()` but project into the new page VM and pass the edit form model. Add `[HttpPost] UpdateProfile(ProfileEditViewModel model)` secured with `[Authorize]` and `[ValidateAntiForgeryToken]`; fetch the current user via `ClaimTypes.NameIdentifier`, validate unique email, update only the allowed fields, hash a new password when provided, `ModelState.IsValid` guard, persist via `_context.SaveChangesAsync()`, and refresh the auth cookie (reuse the sign-in helper already used after login). When validation fails set `ViewData["OpenEditModal"] = true` and return the populated page VM so the modal reopens with errors.
- **View**: Rebuild `Views/Account/Profile.cshtml` into a layout matching the provided card mock: hero header, single profile card with info rows, action row with `Edit` and `Logout`. Keep styles inline inside `@section Styles`: reuse the burgundy palette, add consistent spacing/typography, and ensure buttons share theme classes already seen on login/register. Embed a modal component (`div.profile-modal`) that renders the edit form using tag helpers; disable read-only fields, ensure password inputs are blank, and include client-side script to open/close the modal plus auto-open when `ViewData["OpenEditModal"]` is true. Reuse form validation summary and antiforgery token inside the modal form.

## Staff Stock UI Polish
- **Buttons**: In `Views/Staff/Stock.cshtml`, make the search submit button use `.btn-stock-primary` (or add a new `.btn-stock-accent` defined in the same `<style>` block) so it matches the red buttons showcased in the mockups. Verify the “ไปหน้าจัดการ” CTA (if located on `Views/Staff/Stock.cshtml` or `Views/Staff/Index.cshtml`) also references the same class for theme consistency.
- **Spinner removal**: Extend the page-level CSS at the top of `Views/Staff/Stock.cshtml` to hide number spinners (`input[type=number]::-webkit-inner/outer-spin-button { display:none; }`, `input[type=number] { -moz-appearance: textfield; }`). Scope selectors to `.stock-section` to avoid cross-page effects. Confirm quantity/price inputs and modal numeric fields inherit the spinner-less style.

## Variant Size Validation Tightening
- **Server-side**: Update `ViewModels/Stock/StockModels.cs` so `AddVariantRequest.Size` is `[RegularExpression("^(\\d{1,2}(\\.5)?)$")]` (allows integers or .5 increments) or a stricter digits-only pattern as product requirements dictate, plus `[StringLength(5)]`. Continue to trim inputs in `Controllers/StaffController.cs` before validation so whitespace is ignored. Ensure JSON responses return validation errors using the existing helper.
- **Client-side**: In `Views/Staff/Stock.cshtml`, set the “เพิ่มไซส์ใหม่” field to `input type="number"` with `step="0.5"` (or `inputmode="numeric"` if decimals unsupported) and hook it into the existing `attachNumericGuards` helper to block letters, `+`, and `-`. Update `addVariant()` JS so it explicitly rejects `Number.isNaN(parseFloat(size))`.

## Verification
- Profile: open `/Account/Profile`, confirm new layout, open/close modal, perform successful updates for name/email/phone/address/password, test validation errors (invalid email, mismatch password), ensure modal reopens with error messaging, and verify updated name/email propagate to header.
- Stock UI: visit `/Staff/Stock`, confirm CTA/search buttons display red theme, and ensure numeric inputs (desktop + mobile) no longer show spin buttons while still accepting typed values.
- Variant flow: attempt to add new sizes with letters, symbols, decimals, and valid digits; confirm invalid entries are prevented client-side and server responses return validation errors when bypassed. Re-test adding a valid size/color to ensure no regression in the fetch/refresh flow.
